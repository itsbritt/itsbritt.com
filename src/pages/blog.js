// React
import React from 'react';

// Components
import Layout from '../components/Layout/Layout';

const Blog = () => {
	return (
		<Layout>
			<span>Blog</span>
		</Layout>
	);
};

export default Blog;
