// React
import React from 'react';

// Components
import Layout from '../components/Layout/Layout';

const Resume = () => {
	return (
		<Layout>
			<span>Resume</span>
		</Layout>
	);
};

export default Resume;
